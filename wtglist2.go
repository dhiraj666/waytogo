/*
There are 25 keywords
break	default		func        map      struct
case	defer		goto		package  switch
chan	else		if          range    type
const	fallthrough interface   select
continue for        import      return    var

*/
// Basic structure and components of a go program
// A standalone executable belongs to package main
package main

import "fmt"

func main() {
	fmt.Println("hello, world ")
}
